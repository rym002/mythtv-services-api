version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install -g typescript
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - tsc
  post_build:
    commands:
      - echo ${NPM_TOKEN}
      - npm publish